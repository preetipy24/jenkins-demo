pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build from GitHub'
            }
        }

        stage('Test') {
            steps {
                echo 'Test from GitHub'
            }
        }
    }
}
